name: Lichess Bot Runner

concurrency:
  group: lichess-bot-runner
  cancel-in-progress: true

on:
  workflow_dispatch:
    inputs:
      mode:
        description: 'Bot Mode'
        type: choice
        required: true
        options:
          - normal
          - matchmaking
          - tournament
      tournament_id:
        description: 'Tournament ID (if tournament mode)'
        type: string
        required: false
      team_id:
        description: 'Team ID (optional)'
        type: string
        required: false
      password:
        description: 'Tournament password (optional)'
        type: string
        required: false
  schedule:
    - cron: '0 */6 * * *'  # every 6 hours
  push:
    branches: [ main ]
    paths:
      - '**.py'
      - 'config.yml'

permissions:
  actions: write
  contents: write

jobs:
  bot-runner:
    runs-on: ubuntu-latest
    timeout-minutes: 355  # restart just before GitHub limit

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v5
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install Dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install tenacity pyyaml
          
      - name: Build wordfish (no-LTO, verbose - fixed)
        env:
          WORDFISH_PAT: ${{ secrets.WORDFISH_PAT }}
          GITHUB_WORKSPACE: ${{ github.workspace }}
        shell: bash
        run: |
          set -euo pipefail
          rm -rf wordfish || true

          # Clone (gunakan PAT kalau tersedia)
          if [ -n "${WORDFISH_PAT:-}" ]; then
            git clone "https://x-access-token:${WORDFISH_PAT}@github.com/FayE75/wordfish.git" wordfish
          else
            git clone https://github.com/FayE75/wordfish.git wordfish
          fi

          sudo apt-get update -qq
          sudo apt-get install -y build-essential clang lld llvm-dev pkg-config cmake ninja-build curl unzip || true

          cd wordfish || { echo "wordfish dir missing"; exit 1; }

          if [ -f "scripts/net.sh" ]; then
            cd src && sh ../scripts/net.sh || true
            cd ..
          fi

          if [ -d "src" ]; then
            cd src
            mkdir -p ../build_logs

            echo "🔧 Cleaning"
            make clean || true

            echo "🔧 Forcing NON-LTO build: setting -fno-lto and removing -flto occurrences if present"
            export CC=clang
            export CXX=clang++
            export AR=ar
            export RANLIB=ranlib
            export CFLAGS="-fno-lto ${CFLAGS:-}"
            export CXXFLAGS="-fno-lto ${CXXFLAGS:-}"
            export LDFLAGS="${LDFLAGS:-}"

            if grep -R --line-number --silent "\-flto" .; then
              echo "⚠ Found -flto in Makefiles — stripping occurrences"
              find . -type f -name 'Makefile*' -exec sed -i.bak 's/-flto//g' {} +
            fi

            echo "🔧 Building (no-LTO) - logging to ../build_logs/build.log"

            # --- Robust build: run make, tee output, then check exit code of `make`
            set +e
            make -j2 build ARCH=x86-64-sse41-popcnt COMP=clang V=1 2>&1 | tee ../build_logs/build.log
            MAKE_RC=${PIPESTATUS[0]}
            set -e

            if [ "${MAKE_RC}" -ne 0 ]; then
              echo "❌ Build failed — showing last 300 lines of build.log"
              tail -n 300 ../build_logs/build.log || true

              echo "---- object files summary ----"
              ls -lah *.o || true
              for f in *.o 2>/dev/null || true; do
                echo "---- file info for: $f ----"
                file "$f" || true
              done || true

              echo "❗ Build failed. Build logs saved to wordfish/build_logs (artifact upload step should capture it)."
              exit 1
            fi

            cd ..
          else
            echo "❌ src directory missing; cannot build"
            exit 1
          fi

          # run perft tests (best-effort)
          if [ -d "tests" ]; then
            echo "▶️ Running tests/perft.sh (best-effort)"
            cd tests && bash perft.sh || true
            cd ..
          else
            echo "⚠️ tests directory missing; skipping perft"
          fi

          BIN_PATH=$(find . -type f -executable -iname 'stockfish*' -o -iname 'wordfish*' -print -quit || true)
          if [ -z "$BIN_PATH" ]; then
            BIN_PATH=$(find src -type f -executable -iname 'stockfish*' -o -iname 'wordfish*' -print -quit || true)
          fi

          mkdir -p "${GITHUB_WORKSPACE}/engines"
          if [ -n "$BIN_PATH" ]; then
            echo "✅ Found built engine: $BIN_PATH"
            cp "$BIN_PATH" "${GITHUB_WORKSPACE}/engines/wordfish" || true
            chmod +x "${GITHUB_WORKSPACE}/engines/wordfish" || true
            echo "✅ Engine copied to engines/wordfish"
          else
            echo "⚠️ No engine binary found after build"
          fi

          if [ -d build_logs ]; then
            cp -r build_logs "${GITHUB_WORKSPACE}/build_logs" || true
          fi



      - name: Upload wordfish build logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: wordfish-build-logs
          path: wordfish/build_logs


      - name: Setup Engines
        run: |
          mkdir -p engines
          sudo apt-get update && sudo apt-get install -y unzip || true
          if [ -f "engines/wordfish" ]; then chmod +x engines/wordfish; fi
          if [ -f "engines/fsf" ]; then chmod +x engines/fsf; fi
          if [ -f "engines/ShashChess39" ]; then chmod +x engines/ShashChess39; fi

      - name: Inject Lichess Bot Token
        env:
          LICHESS_TOKEN: ${{ secrets.LICHESS_TOKEN }}
        run: |
          python3 << 'EOF'
          import os, yaml, sys
          token = os.environ.get("LICHESS_TOKEN")
          if not token:
              print("ERROR: LICHESS_TOKEN secret is missing!")
              sys.exit(1)
          cfg_path = "config.yml"
          cfg = {}
          if os.path.exists(cfg_path):
              with open(cfg_path,"r") as f:
                  cfg = yaml.safe_load(f) or {}
          cfg["token"] = token
          with open(cfg_path,"w") as f:
              yaml.dump(cfg,f, default_flow_style=False)
          print("✅ Token injected successfully")
          EOF

      - name: Launch Bot
        run: |
          CMD_BASE="python3 -u user_interface.py"
          MODE="${{ github.event.inputs.mode }}"
          TOUR_ID="${{ github.event.inputs.tournament_id }}"
          TEAM_ID="${{ github.event.inputs.team_id }}"
          PASSWORD="${{ github.event.inputs.password }}"

          if [[ "$MODE" == "matchmaking" ]]; then
              CMD="$CMD_BASE matchmaking"
              echo "🤖 Running command: $CMD"
              eval "$CMD"

          elif [[ "$MODE" == "tournament" ]]; then
              if [[ -z "$TOUR_ID" ]]; then
                  echo "⚠️ Tournament ID missing. Skipping."
              else
                  ARGS="$TOUR_ID"
                  [[ -n "$TEAM_ID" ]] && ARGS="$ARGS $TEAM_ID"
                  [[ -n "$PASSWORD" ]] && ARGS="$ARGS $PASSWORD"

                  echo "🤖 Joining tournament: $ARGS"
                  eval "$CMD_BASE \"tournament $ARGS\""
              fi

          else
              echo "⚠️ Mode not recognized. Running normal mode."
              eval "$CMD_BASE"
          fi

      - name: Auto-Restart After Timeout
        run: |
          echo "Auto-restart step present but control must be implemented inside the bot process/launcher if needed."
